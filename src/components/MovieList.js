// *****PARENT*****
import MovieItem from '../components/MovieItem'

const MovieList = () => {
    return (
        <section class="movie-list-continer">
            <MovieItem />
            <MovieItem />
            <MovieItem />
        </section>
    )
}

export default MovieList
